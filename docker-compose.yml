services:
  linkpage:
    build: .
    ports:
      - "127.0.0.1:${HOST_PORT:-3100}:${PORT:-3000}"
    volumes:
      - ./data:/app/data
      - ./public/uploads:/app/public/uploads
      - ./.env:/app/.env
    restart: unless-stopped
